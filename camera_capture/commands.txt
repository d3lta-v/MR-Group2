cd ~/ros2_ws/src
mkdir -p camera_capture/camera_capture
mkdir -p camera_capture/resource

# Create empty files
touch camera_capture/camera_capture/__init__.py
touch camera_capture/resource/camera_capture

# Create the main node file
# (paste the Python code from the artifact here)
nano camera_capture/camera_capture/camera_capture_node.py

# Create the other files
nano camera_capture/package.xml
nano camera_capture/setup.py
nano camera_capture/setup.cfg